import { SearchbytitlePipe } from './searchbytitle.pipe';

describe('SearchbytitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbytitlePipe();
    expect(pipe).toBeTruthy();
  });
});
